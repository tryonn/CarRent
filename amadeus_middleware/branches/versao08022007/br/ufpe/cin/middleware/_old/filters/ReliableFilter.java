package br.ufpe.cin.middleware._old.filters;
/**
 * Classe que representa o filtro usado pelo conector confiável
 * ao receber suas notificações
 * 
 * @author Eliaquim Lima (elsn@cin.ufpe.br)
 * @author Marcelo Queiroz (mrsq@cin.ufpe.br)
 *
 */
/*
public class ReliableFilter implements NotificationFilter {

	public boolean isNotificationEnabled(Notification arg0) {
		boolean enabled = false;
		if (arg0.getMessage().equals(ConfiguratorMessages.RELIABLE_SEND) ||
				arg0.getMessage().equals(ConfiguratorMessages.RELIABLE_SENDTO) ||
					arg0.getMessage().equals(ConfiguratorMessages.RELIABLE_RECEIVE) ||
					arg0.getMessage().equals(ConfiguratorMessages.RELIABLE_CLOSE) || 
					arg0.getMessage().equals(ConfiguratorMessages.RELIABLE_LISTEN) ||
					arg0.getMessage().equals(ConfiguratorMessages.RELIABLE_SENDTOALL)) 
			{
				enabled = true;
			}
		return enabled;
	}

}*/
