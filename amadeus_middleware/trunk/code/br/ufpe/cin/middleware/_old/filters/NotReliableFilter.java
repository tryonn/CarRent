package br.ufpe.cin.middleware._old.filters;
/**
 * Classe que representa o filtro usado pelo conector não-confiável
 * ao receber suas notificações.
 * 
 * @author Eliaquim Lima (elsn@cin.ufpe.br)
 * @author Marcelo Queiroz (mrsq@cin.ufpe.br)
 *
 */
/*public class NotReliableFilter implements NotificationFilter {

	public boolean isNotificationEnabled(Notification arg0) {
		boolean enabled = false;
		if (arg0.getMessage().equals(ConfiguratorMessages.NOTRELIABLE_SEND)
				|| arg0.getMessage().equals(ConfiguratorMessages.NOTRELIABLE_RECEIVE)
				|| arg0.getMessage().equals(ConfiguratorMessages.NOTRELIABLE_CLOSE))
				{
					enabled = true;
				}
	
		return enabled;
	}

}*/
